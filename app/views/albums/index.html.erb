<% if current_user %>
  <h2>Albums</h2>
  <%= link_to "Add album", new_album_path %>
  <ul>
    <% @albums.each do |a| %>
      <li><%= link_to "#{a.title} — #{a.artist}", album_path(a) %></li>
    <% end %>
  </ul>
<% else %>
  <h2>Albums</h2>
  <p>You must <%= link_to "sign in", new_session_path %> or <%= link_to "sign up", new_user_path %> to view albums.</p>
<% end %>
