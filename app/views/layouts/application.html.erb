<!DOCTYPE html>
<html>
  <head>
    <title>Aura - Where Music Taste Comes to Life</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="header-enhanced">
      <div class="header-brand">
        <h1><%= link_to "♫ Aura", root_path %></h1>
        <span class="tagline">Where Music Taste Comes to Life</span>
      </div>
      
      <nav class="header-nav">
        <% if current_user %>
          <div class="nav-links">
            <%= link_to "Discover", root_path, class: "nav-link" %>
            <%= link_to "Find Friends", friends_path, class: "nav-link" %>
            <%= link_to "Profile", user_path(current_user), class: "nav-link" %>
          </div>
          
          <div class="user-menu">
            <div class="user-info">
              <span class="user-name"><%= current_user.name %></span>
              <% if current_user.lastfm_connected %>
                <span class="lastfm-indicator" title="Last.fm Connected">♫</span>
              <% end %>
            </div>
            <%= button_to "Sign Out", session_path, method: :delete, class: "btn btn-ghost", form: { class: "inline-form" } %>
          </div>
        <% else %>
          <div class="auth-links">
            <%= link_to "Sign In", new_session_path, class: "nav-link" %>
            <%= link_to "Sign Up", new_user_path, class: "btn btn-primary btn-sm" %>
          </div>
        <% end %>
      </nav>
    </header>

    <% flash.each do |k,v| %>
      <div class="flash-message <%= k %>">
        <span class="flash-text"><%= v %></span>
      </div>
    <% end %>

    <main class="main-content">
      <%= yield %>
    </main>

    <footer class="app-footer">
      <p>&copy; 2025 Aura. Where Music Taste Comes to Life.</p>
    </footer>

  </body>
</html>
